+++
title = "onboard"
description = "CLI reference for agentgraph onboard."
nav_title = "onboard"
section = "Reference"
order = 22
summary = "`agentgraph onboard` walks the installed connectors and launches each authentication flow in sequence."
output = "commands/onboard.html"
source_path = "docs-src/commands/onboard.md"
+++

## Synopsis

```bash
agentgraph onboard
```

## Use it for

- first-time setup
- re-running auth for several platforms in one pass

## Example

```bash
agentgraph onboard
```
