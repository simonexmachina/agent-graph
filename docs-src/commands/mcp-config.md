+++
title = "mcp-config"
description = "CLI reference for agentgraph mcp-config."
nav_title = "mcp-config"
section = "Reference"
order = 25
summary = "`agentgraph mcp-config` prints the MCP client snippet for stdio clients and shows the remote transport pattern for ChatGPT."
output = "commands/mcp-config.html"
source_path = "docs-src/commands/mcp-config.md"
+++

## Synopsis

```bash
agentgraph mcp-config
```

## Use it for

- Claude Desktop config
- Claude Code config
- ChatGPT developer mode guidance for SSE or streaming HTTP

## Example

```bash
agentgraph mcp-config
```
