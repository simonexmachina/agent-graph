+++
title = "serve"
description = "CLI reference for agentgraph serve."
nav_title = "serve"
section = "Reference"
order = 24
summary = "`agentgraph serve` starts the local HTTP server, graph viewer, browser observation endpoint, and connector pollers."
output = "commands/serve.html"
source_path = "docs-src/commands/serve.md"
+++

## Synopsis

```bash
agentgraph serve [--reload]
```

## Example

```bash
agentgraph serve
agentgraph serve --reload
```
