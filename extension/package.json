{
  "name": "agentgraph-extension",
  "version": "0.1.2",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc && node scripts/bundle.mjs",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test test/*.test.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.300",
    "typescript": "^5.4.5",
    "esbuild": "^0.21.4"
  }
}
